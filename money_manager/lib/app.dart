import 'package:flutter/material.dart';

class MoneyManagerApp extends MaterialApp {
  const MoneyManagerApp({super.key})
      : super(
          home: const Scaffold(
            body: Center(
              child: Text('Hello World'),
            ),
          ),
        );
}
